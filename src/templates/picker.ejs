<div class="<%= classes.wrapper %>">
  <div class="<%= classes.picker %>">
    <% if (plugins) { %>
      <div data-placeholder="plugins"></div>
    <% } %>
    <div data-view="search"></div>
    <div class="<%= classes.content %>">
      <div data-view="emojiArea"></div>
    </div>
  </div>
</div>